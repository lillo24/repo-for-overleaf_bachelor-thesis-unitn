
\chapter{Conclusions and Future Work}

\section{Experimental Results}
The main retrieval results will compare the BM25 baseline against the LLM-rewritten query condition. The table will be filled after running the final evaluation.

\begin{table}[h]
\centering
\begin{tabular}{lccc}
\toprule
\textbf{Condition} & \textbf{Success@1} & \textbf{Success@5} & \textbf{MRR} \\
\midrule
BM25 original query & TODO & TODO & TODO \\
BM25 rewritten query & TODO & TODO & TODO \\
\bottomrule
\end{tabular}
\caption{Retrieval performance comparison.}
\label{tab:results-main}
\end{table}

The expected interpretation is not only whether query rewriting improves the average scores, but also in which classes of queries it helps. The most relevant cases are those where the student query is understandable to a human but lexically distant from the terminology used in official sources.

\section{TODO / INTEGRATE: Rewrite Stability Across Repeated Runs}
This is a good stability result. The rewrite is not perfectly deterministic, but the variation is small enough to be acceptable.

\begin{table}[h]
\centering
\begin{tabular}{lccc}
\toprule
\textbf{Condition} & \textbf{Hit@1} & \textbf{Hit@3} & \textbf{MRR} \\
\midrule
Frozen rewrite, strict & 38.5--46.2\% & 76.9--84.6\% & 0.551--0.609 \\
Frozen rewrite, lenient & 65.4--69.2\% & 80.8--92.3\% & 0.718--0.801 \\
\bottomrule
\end{tabular}
\caption{Observed range across repeated runs with the same rewrite instruction.}
\label{tab:rewrite-stability}
\end{table}

Because the dataset is small, one case corresponds to approximately 3.8 percentage points, so most changes represent only one to three cases moving around in the ranking.

The important thesis-level conclusion is that, across repeated runs with the same rewrite instruction, frozen rewriting consistently outperformed the original-query baseline by a large margin, although individual ranks varied slightly.

These three runs should be used as evidence of directional robustness, not as evidence that the exact metric values are perfectly stable. The safest reporting strategy is either to report the average over the three runs or to state that repeated runs remained in the same performance band.

\section{Discussion and Error Analysis}
The error analysis should explain why query rewriting helps or fails. Possible failure classes include:

\begin{itemize}
    \item \textbf{Missing source}: the correct official source is not in the indexed corpus;
    \item \textbf{Bad source granularity}: the document is indexed, but the relevant information is hidden in a long segment or split across segments;
    \item \textbf{Ambiguous query}: the question is course-specific but does not specify the degree programme;
    \item \textbf{Rewrite drift}: the LLM changes the original intent or adds unsupported assumptions;
    \item \textbf{Lexical mismatch remains}: the rewritten query still does not match the official wording;
    \item \textbf{Duplicate yearly documents}: old and new versions compete in the ranking;
    \item \textbf{Right retrieval, wrong downstream answer}: relevant mainly for the broader product prototype, not for the core retrieval experiment.
\end{itemize}

This analysis is important because the goal of the thesis is not simply to report a single score. The goal is to understand whether a lightweight LLM layer can improve a deterministic retrieval pipeline, and under which conditions this design is useful.

\section{Product Context}
This thesis is extracted from a broader product-oriented prototype for a university bureaucracy assistant. The broader system includes controlled source acquisition, manifest/versioning, HTML/PDF extraction, BM25 indexing, a backend service, a frontend interface, and debugging tools for inspecting acquisition and extraction.

The product-oriented goal is to support students and administrative staff by providing a trustworthy interface over official university sources. However, the thesis isolates one measurable component: whether LLM query rewriting improves retrieval quality.

\section{Limitations}
The current thesis focuses on retrieval rather than full answer generation. This keeps the experiment measurable, but it does not fully evaluate chatbot behavior. Citation correctness, refusal behavior, answer faithfulness, and user satisfaction are therefore outside the core evaluation unless added as secondary analysis.

Another limitation is source coverage. If the correct document is missing from the corpus, query rewriting cannot solve the problem. In such cases, the limitation belongs to acquisition and corpus completeness, not only to retrieval.

Finally, query rewriting itself can introduce errors. A rewritten query may be lexically stronger while semantically drifting from the original student intent. This is why manual relevance judgement and error analysis remain necessary.

\section{TODO / INTEGRATE: Italian Stemmed and Analyzed Fallback Field}
A possible improvement, not included in the current experiment, is to add an Italian stemmed or analyzed fallback field to the BM25 index. This would be applied to both experimental conditions: the original-query baseline and the LLM-rewritten query condition.

The motivation is that some retrieval failures may be caused by small morphological differences rather than by a deeper semantic mismatch. For example, a query term may appear in one form in the rewritten query and in another form in the official source, such as singular/plural or masculine/feminine variants. In these cases, changing the rewrite prompt is probably the wrong fix, because it risks hard-coding domain-specific terms and adding noise. A better retrieval-side improvement would be to search both exact fields and Italian-analyzed fields, while keeping exact title, heading, and body fields boosted.

This would create a useful future comparison:

\begin{itemize}
    \item BM25 with exact fields only;
    \item BM25 with exact fields plus Italian stemmed/analyzed fallback;
    \item BM25 with LLM query rewriting over exact fields;
    \item BM25 with LLM query rewriting over exact fields plus Italian stemmed/analyzed fallback.
\end{itemize}

This distinction matters because query rewriting and linguistic analysis solve different parts of the retrieval problem. Query rewriting tries to bridge the student's informal intent with institutional wording. Stemming or linguistic analysis instead improves robustness to surface-form variation inside the same language.

\section{Conclusions}
This thesis evaluates whether a low-cost LLM can improve deterministic retrieval by rewriting informal student queries into better BM25 search queries. The broader motivation is that, in institutional question answering, answer fluency is not sufficient: correctness depends on retrieving the right official context.

The central contribution is therefore not a generic chatbot, but an empirical study of a lightweight AI layer that may improve a hard-coded retrieval pipeline while preserving inspectability and provenance.

\section{Future Work}
Future work may include:

\begin{itemize}
    \item adding an Italian stemmed or analyzed fallback field while keeping exact fields boosted;
    \item hybrid retrieval combining BM25 and vector search;
    \item reranking retrieved results;
    \item structured databases for hard constraints such as ECTS requirements;
    \item stronger citation and refusal evaluation for generated answers;
    \item broader source acquisition across additional university domains;
    \item user studies with students and administrative staff;
    \item full assistant evaluation including answer correctness and faithfulness.
\end{itemize}